import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color bgPrimary = Color(0xFF121212);
  static const Color bgSurface = Color(0xFF1C1C1C);
  static const Color bgElevated = Color(0xFF242424);
  static const Color bgEmpty = Color(0xFF2E2E2E);

  // Accent
  static const Color accent = Color(0xFFC8F135);
  static const Color accentDim = Color(0x66C8F135);
  static const Color accentSubtle = Color(0x12C8F135);

  // Text
  static const Color textPrimary = Color(0xFFF0F0F0);
  static const Color textMuted = Color(0x47F0F0F0);
  static const Color textDisabled = Color(0x73FFFFFF);
  static const Color textInverse = Color(0xFF121212);

  // Borders
  static const Color borderSubtle = Color(0x12FFFFFF);
  static const Color borderMid = Color(0x1FFFFFFF);
  static const Color borderCard = Color(0x0FFFFFFF);

  // Quest card
  static const Color questCardBg = accent;
  static const Color questCardOverlay = Color(0x1A121212);
  static const Color questCardTextMuted = Color(0x8C121212);
  static const Color questCardTextDim = Color(0x73121212);

  // Profile card
  static const Color profileCardBg = accent;
}
